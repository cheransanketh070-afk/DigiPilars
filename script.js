(()=>{const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce)document.documentElement.classList.add('reduced-motion');
const menu=document.querySelector('.menu'),mobileNav=document.querySelector('#mobileNav');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));mobileNav.hidden=open});mobileNav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobileNav.hidden=true;menu.setAttribute('aria-expanded','false')}));
const canvas=document.querySelector('#hero3d');let renderer,scene,camera,core,halo,particles;const pointer={x:0,y:0,tx:0,ty:0};
if(canvas&&window.THREE){scene=new THREE.Scene();camera=new THREE.PerspectiveCamera(42,1,.1,100);camera.position.set(0,0,7.4);renderer=new THREE.WebGLRenderer({canvas,antialias:true,alpha:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(window.devicePixelRatio,1.5));renderer.setClearColor(0x050608,0);
const group=new THREE.Group();scene.add(group);core=new THREE.Mesh(new THREE.IcosahedronGeometry(1.65,2),new THREE.MeshPhysicalMaterial({color:0x0a1111,metalness:.75,roughness:.18,transmission:.18,transparent:true,opacity:.96}));group.add(core);
const wire=new THREE.Mesh(new THREE.IcosahedronGeometry(1.73,2),new THREE.MeshBasicMaterial({color:0x74f6d1,wireframe:true,transparent:true,opacity:.12}));group.add(wire);
halo=new THREE.Mesh(new THREE.TorusGeometry(2.15,.008,8,160),new THREE.MeshBasicMaterial({color:0x74f6d1,transparent:true,opacity:.55}));halo.rotation.x=Math.PI/2.2;group.add(halo);const halo2=halo.clone();halo2.rotation.x=.55;halo2.rotation.y=.75;halo2.scale.setScalar(.82);halo2.material=halo.material.clone();halo2.material.opacity=.25;group.add(halo2);
const pCount=innerWidth<700?480:900,positions=new Float32Array(pCount*3);for(let i=0;i<pCount;i++){const r=3.2+Math.random()*4.5,a=Math.random()*Math.PI*2,z=(Math.random()-.5)*5;positions[i*3]=Math.cos(a)*r;positions[i*3+1]=Math.sin(a)*r*.55;positions[i*3+2]=z}const pGeo=new THREE.BufferGeometry();pGeo.setAttribute('position',new THREE.BufferAttribute(positions,3));particles=new THREE.Points(pGeo,new THREE.PointsMaterial({color:0x74f6d1,size:.012,transparent:true,opacity:.38,sizeAttenuation:true}));scene.add(particles);scene.add(new THREE.AmbientLight(0x9ffff0,.35));const light=new THREE.PointLight(0x74f6d1,3.5,12);light.position.set(3,2,4);scene.add(light);const light2=new THREE.PointLight(0x3f5960,3,10);light2.position.set(-3,-2,3);scene.add(light2);
const resize=()=>{const w=canvas.clientWidth,h=canvas.clientHeight;renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix()};resize();addEventListener('resize',resize,{passive:true});addEventListener('pointermove',e=>{pointer.tx=(e.clientX/innerWidth-.5)*2;pointer.ty=(e.clientY/innerHeight-.5)*2;const g=document.querySelector('.cursor-glow');if(g){g.style.opacity='.65';g.style.left=e.clientX+'px';g.style.top=e.clientY+'px'}},{passive:true});
const render=()=>{if(!reduce){pointer.x+=(pointer.tx-pointer.x)*.04;pointer.y+=(pointer.ty-pointer.y)*.04;group.rotation.y+=.0018;group.rotation.x=pointer.y*.08;group.position.x=pointer.x*.12;group.position.y=-pointer.y*.08;wire.rotation.y-=.0022;halo.rotation.z+=.0018;halo2.rotation.z-=.0011;particles.rotation.y+=.00025;particles.rotation.x=pointer.y*.015;core.rotation.y+=.0011;core.rotation.x+=.0006}renderer.render(scene,camera);requestAnimationFrame(render)};render()}
if(window.gsap&&window.ScrollTrigger&&!reduce){gsap.registerPlugin(ScrollTrigger);const lenis=window.Lenis?new Lenis({duration:1.05,smoothWheel:true,syncTouch:false}):null;if(lenis){const tick=t=>{lenis.raf(t*1000);requestAnimationFrame(tick)};requestAnimationFrame(tick);lenis.on('scroll',ScrollTrigger.update)}gsap.to('.progress span',{width:'100%',ease:'none',scrollTrigger:{scrub:true,start:'top top',end:'bottom bottom'}});gsap.utils.toArray('.reveal').forEach(el=>gsap.fromTo(el,{y:42,opacity:0},{y:0,opacity:1,duration:.85,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 88%',once:true}}));gsap.utils.toArray('.service-card').forEach((card,i)=>gsap.fromTo(card,{y:70,opacity:0},{y:0,opacity:1,duration:.8,delay:i*.05,scrollTrigger:{trigger:card,start:'top 86%',once:true}}));gsap.utils.toArray('.destination-visual').forEach(el=>gsap.fromTo(el,{y:40,opacity:.25},{y:0,opacity:1,duration:1.2,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 85%',once:true}}));gsap.utils.toArray('.hero-card').forEach((el,i)=>gsap.fromTo(el,{y:50,opacity:0,scale:.92},{y:0,opacity:1,scale:1,duration:1.1,delay:.65+i*.12,ease:'power3.out'}));gsap.fromTo('.hero-copy',{y:35,opacity:0},{y:0,opacity:1,duration:1.1,delay:.25,ease:'power3.out'});gsap.to('.system-orbit',{y:-90,scrollTrigger:{trigger:'.system',start:'top bottom',end:'bottom top',scrub:1}});gsap.utils.toArray('.destination').forEach(s=>gsap.to(s.querySelector('.destination-visual'),{x:24,scrollTrigger:{trigger:s,start:'top bottom',end:'bottom top',scrub:1}}))}
if(!reduce&&matchMedia('(pointer:fine)').matches){document.querySelectorAll('.card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(700px) rotateX(${y*-8}deg) rotateY(${x*10}deg) translateZ(8px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});document.querySelectorAll('.magnetic').forEach(btn=>{btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect();btn.style.transform=`translate(${(e.clientX-(r.left+r.width/2))*.12}px,${(e.clientY-(r.top+r.height/2))*.12}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')})}
const aiButton=document.querySelector('#aiButton'),aiPanel=document.querySelector('#aiPanel'),aiClose=document.querySelector('#aiClose'),aiAnswer=document.querySelector('#aiAnswer');const setAi=open=>{aiPanel.hidden=!open;aiButton.setAttribute('aria-expanded',String(open));if(open)aiPanel.querySelector('button')?.focus()};aiButton?.addEventListener('click',()=>setAi(aiPanel.hidden));aiClose?.addEventListener('click',()=>setAi(false));document.querySelectorAll('#aiQuestions button').forEach(q=>q.addEventListener('click',()=>aiAnswer.textContent=q.dataset.answer||''));document.querySelector('#contactForm')?.addEventListener('submit',e=>{e.preventDefault();const form=e.currentTarget,msg=document.querySelector('#formMessage');if(!form.checkValidity()){form.reportValidity();return}msg.textContent='Inquiry captured. Connect this form to your preferred inbox/CRM endpoint before launch.'})})();